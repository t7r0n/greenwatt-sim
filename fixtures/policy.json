{
  "company": "DeepAware AI",
  "repo": "greenwatt-sim",
  "metrics": [
    "deepaware_coverage",
    "promises_risk",
    "energy_precision",
    "waste_latency"
  ],
  "failure_modes": [
    "deepaware_drift",
    "promises_gap",
    "energy_misroute",
    "waste_blindspot"
  ],
  "domain_archetypes": [
    {
      "name": "deepaware evidence replay",
      "trigger": "deepaware signal changes while promises context is stale",
      "expected": "block release until cited evidence is regenerated"
    },
    {
      "name": "promises boundary probe",
      "trigger": "promises handoff crosses a policy or trust boundary",
      "expected": "route to reviewer with evidence packet"
    },
    {
      "name": "energy regression harness",
      "trigger": "energy behavior regresses against the last accepted fixture",
      "expected": "open a regression issue with trace and benchmark delta"
    },
    {
      "name": "waste operator packet",
      "trigger": "waste output needs a human-readable audit packet",
      "expected": "accept only if decision claims cite fixture evidence"
    }
  ],
  "minimum_cases": 144,
  "requires_evidence_markers": true
}